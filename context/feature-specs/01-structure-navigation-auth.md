Read `CLAUDE.md` before starting and fetch the relevant design files and it's read its readme from https://api.anthropic.com/v1/design/h/1RKOTB6F-NQ5rumr_XqTeg?open_file=SwishDish.html

In the first step we're building out the project structure, the design system and sign up and sign in screan and API calls.

The golang API doesn't exist yet so you can declare the necessary endpoint while implementing the adapter.

Add the following screens
- Sign in
- Sign up

Add the folder structure

If components are needed add them as reusable components for example a resuable button component.

### Check when done
- All created components import without errors
- JWT is stored correctly when signing in
